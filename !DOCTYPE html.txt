<!DOCTYPE html>
<html lang="es">
<head>
<meta charset="UTF-8">
<meta name="viewport" content="width=device-width, initial-scale=1.0">
<title>MaruProyectAI — Ecosistema Digital de Maru Rodriguez</title>
<link href="https://fonts.googleapis.com/css2?family=Syne:wght@400;600;700;800&family=DM+Sans:ital,opsz,wght@0,9..40,300;0,9..40,400;0,9..40,500;1,9..40,300&display=swap" rel="stylesheet">
<style>
  :root {
    --turquesa: #00B4D8;
    --turquesa-deep: #0077B6;
    --turquesa-glow: rgba(0,180,216,0.15);
    --dark: #03071E;
    --dark-2: #080F24;
    --dark-3: #0D1A38;
    --dark-4: #122040;
    --gold: #FFB703;
    --white: #F0F6FF;
    --gray: #7A8FAD;
    --gray-light: #A8BBCC;
  }

  * { margin: 0; padding: 0; box-sizing: border-box; }
  html { scroll-behavior: smooth; }

  body {
    font-family: 'DM Sans', sans-serif;
    background: var(--dark);
    color: var(--white);
    overflow-x: hidden;
    cursor: none;
  }

  /* ── CURSOR ── */
  .cursor {
    width: 10px; height: 10px;
    background: var(--turquesa);
    border-radius: 50%;
    position: fixed;
    pointer-events: none;
    z-index: 9999;
    mix-blend-mode: screen;
    transition: transform 0.1s;
  }
  .cursor-ring {
    width: 34px; height: 34px;
    border: 1.5px solid rgba(0,180,216,0.6);
    border-radius: 50%;
    position: fixed;
    pointer-events: none;
    z-index: 9998;
    transition: all 0.18s ease;
  }

  /* ── NAV ── */
  nav {
    position: fixed; top: 0; left: 0; right: 0;
    z-index: 200;
    padding: 18px 5%;
    display: flex;
    justify-content: space-between;
    align-items: center;
    background: rgba(3,7,30,0.85);
    backdrop-filter: blur(24px);
    border-bottom: 1px solid rgba(0,180,216,0.1);
  }

  .nav-logo {
    font-family: 'Syne', sans-serif;
    font-weight: 800;
    font-size: 1.35rem;
    color: var(--turquesa);
    letter-spacing: -0.5px;
    text-decoration: none;
  }
  .nav-logo span { color: var(--white); }

  .nav-links {
    display: flex; gap: 36px;
    list-style: none; align-items: center;
  }
  .nav-links a {
    color: var(--gray-light);
    text-decoration: none;
    font-size: 0.84rem;
    font-weight: 500;
    letter-spacing: 0.4px;
    transition: color 0.25s;
  }
  .nav-links a:hover { color: var(--turquesa); }

  .nav-cta {
    background: var(--turquesa) !important;
    color: var(--dark) !important;
    padding: 9px 22px !important;
    border-radius: 100px;
    font-weight: 700 !important;
    transition: all 0.25s !important;
    letter-spacing: 0 !important;
  }
  .nav-cta:hover {
    background: var(--white) !important;
    transform: scale(1.04);
    box-shadow: 0 8px 24px rgba(0,180,216,0.3);
  }

  .nav-toggle {
    display: none;
    flex-direction: column;
    gap: 5px;
    cursor: pointer;
    padding: 4px;
  }
  .nav-toggle span {
    display: block; width: 24px; height: 2px;
    background: var(--white);
    border-radius: 2px;
    transition: all 0.3s;
  }

  /* ── HERO ── */
  .hero {
    min-height: 100vh;
    display: flex; align-items: center; justify-content: center;
    position: relative; overflow: hidden;
    padding: 130px 5% 90px;
  }

  .hero-grid {
    position: absolute; inset: 0;
    background-image:
      linear-gradient(rgba(0,180,216,0.04) 1px, transparent 1px),
      linear-gradient(90deg, rgba(0,180,216,0.04) 1px, transparent 1px);
    background-size: 60px 60px;
  }

  .hero-grid::after {
    content: '';
    position: absolute; inset: 0;
    background: radial-gradient(ellipse 80% 60% at 50% 50%, transparent 40%, var(--dark) 100%);
  }

  .hero-orb-1 {
    position: absolute;
    width: 700px; height: 700px;
    background: radial-gradient(circle, rgba(0,180,216,0.10), transparent 68%);
    top: -150px; right: -150px;
    border-radius: 50%;
    animation: floatOrb 9s ease-in-out infinite;
  }

  .hero-orb-2 {
    position: absolute;
    width: 500px; height: 500px;
    background: radial-gradient(circle, rgba(0,119,182,0.08), transparent 68%);
    bottom: -80px; left: -80px;
    border-radius: 50%;
    animation: floatOrb 12s ease-in-out infinite reverse;
  }

  @keyframes floatOrb {
    0%, 100% { transform: translateY(0px) scale(1); }
    50% { transform: translateY(-28px) scale(1.03); }
  }

  .hero-content {
    position: relative; z-index: 2;
    max-width: 860px; text-align: center;
  }

  .hero-badge {
    display: inline-flex; align-items: center; gap: 8px;
    background: rgba(0,180,216,0.08);
    border: 1px solid rgba(0,180,216,0.28);
    padding: 7px 20px; border-radius: 100px;
    font-size: 0.75rem; color: var(--turquesa);
    letter-spacing: 1.2px; text-transform: uppercase;
    font-weight: 600; margin-bottom: 30px;
    animation: fadeInDown 0.7s ease both;
  }

  .badge-dot {
    width: 6px; height: 6px;
    background: var(--turquesa); border-radius: 50%;
    animation: pulse 2s infinite;
  }

  @keyframes pulse {
    0%, 100% { opacity: 1; transform: scale(1); }
    50% { opacity: 0.4; transform: scale(1.6); }
  }

  .hero h1 {
    font-family: 'Syne', sans-serif;
    font-size: clamp(2.6rem, 5.5vw, 4.8rem);
    font-weight: 800;
    line-height: 1.06;
    margin-bottom: 22px;
    animation: fadeInUp 0.7s ease 0.15s both;
  }

  .hero h1 .highlight {
    color: var(--turquesa);
    position: relative;
  }
  .hero h1 .highlight::after {
    content: '';
    position: absolute;
    bottom: 2px; left: 0; right: 0; height: 3px;
    background: linear-gradient(90deg, var(--turquesa), transparent);
    border-radius: 2px;
  }

  .hero-sub {
    font-size: 1.15rem;
    color: var(--gray-light);
    line-height: 1.75;
    max-width: 580px; margin: 0 auto 44px;
    font-weight: 300;
    animation: fadeInUp 0.7s ease 0.3s both;
  }

  .hero-ctas {
    display: flex; gap: 14px;
    justify-content: center; flex-wrap: wrap;
    animation: fadeInUp 0.7s ease 0.45s both;
  }

  .btn-primary {
    background: var(--turquesa); color: var(--dark);
    padding: 15px 34px; border-radius: 100px;
    font-weight: 700; font-size: 0.95rem;
    text-decoration: none;
    display: inline-flex; align-items: center; gap: 8px;
    transition: all 0.28s;
    font-family: 'Syne', sans-serif;
    letter-spacing: 0.2px;
  }
  .btn-primary:hover {
    background: var(--white);
    transform: translateY(-3px);
    box-shadow: 0 20px 40px rgba(0,180,216,0.28);
  }

  .btn-secondary {
    background: transparent; color: var(--white);
    padding: 15px 34px; border-radius: 100px;
    font-weight: 600; font-size: 0.95rem;
    text-decoration: none;
    border: 1.5px solid rgba(255,255,255,0.18);
    transition: all 0.28s;
    font-family: 'Syne', sans-serif;
  }
  .btn-secondary:hover {
    border-color: var(--turquesa);
    color: var(--turquesa);
    transform: translateY(-3px);
  }

  /* ── STATS ── */
  .stats {
    padding: 56px 5%;
    display: flex; justify-content: center;
    gap: 0;
    background: var(--dark-2);
    border-top: 1px solid rgba(0,180,216,0.08);
    border-bottom: 1px solid rgba(0,180,216,0.08);
    flex-wrap: wrap;
  }

  .stat {
    text-align: center;
    padding: 16px 48px;
    position: relative;
  }

  .stat:not(:last-child)::after {
    content: '';
    position: absolute; right: 0; top: 20%; bottom: 20%;
    width: 1px;
    background: rgba(255,255,255,0.07);
  }

  .stat-number {
    font-family: 'Syne', sans-serif;
    font-size: 2.4rem; font-weight: 800;
    color: var(--turquesa); display: block;
    line-height: 1;
  }
  .stat-label {
    font-size: 0.78rem; color: var(--gray);
    text-transform: uppercase; letter-spacing: 1px;
    margin-top: 6px; display: block;
  }

  /* ── SECTION COMMON ── */
  section { padding: 100px 5%; }

  .section-badge {
    display: inline-block;
    background: rgba(0,180,216,0.08);
    border: 1px solid rgba(0,180,216,0.2);
    color: var(--turquesa);
    padding: 5px 15px; border-radius: 100px;
    font-size: 0.72rem; letter-spacing: 1.5px;
    text-transform: uppercase; font-weight: 600;
    margin-bottom: 18px;
  }

  .section-title {
    font-family: 'Syne', sans-serif;
    font-size: clamp(1.8rem, 3.2vw, 2.7rem);
    font-weight: 800; line-height: 1.15;
    margin-bottom: 14px;
  }

  .section-sub {
    color: var(--gray-light);
    font-size: 1rem; line-height: 1.75;
    max-width: 540px; font-weight: 300;
  }

  /* ── PROJECTS ── */
  .projects { background: var(--dark-2); }

  .projects-header { text-align: center; margin-bottom: 64px; }
  .projects-header .section-sub { margin: 0 auto; }

  .projects-grid {
    display: grid;
    grid-template-columns: repeat(3, 1fr);
    grid-template-rows: auto auto;
    gap: 22px;
    max-width: 1100px; margin: 0 auto;
  }

  .project-card-featured {
    grid-column: 1 / -1;
    background: var(--dark-3);
    border: 1.5px solid rgba(0,180,216,0.35);
    border-radius: 20px;
    padding: 40px 44px;
    display: flex; gap: 40px; align-items: center;
    transition: all 0.4s;
    position: relative; overflow: hidden;
  }

  .project-card-featured::before {
    content: '';
    position: absolute; top: 0; left: 0; right: 0; height: 3px;
    background: linear-gradient(90deg, var(--turquesa), var(--turquesa-deep));
  }

  .project-card-featured:hover {
    transform: translateY(-5px);
    box-shadow: 0 30px 60px rgba(0,0,0,0.35);
    border-color: rgba(0,180,216,0.6);
  }

  .featured-emoji { font-size: 3.5rem; flex-shrink: 0; }

  .featured-body { flex: 1; }

  .featured-body .project-name {
    font-family: 'Syne', sans-serif;
    font-size: 1.6rem; font-weight: 800;
    margin-bottom: 10px;
  }

  .featured-body .project-desc {
    color: var(--gray-light);
    font-size: 1rem; line-height: 1.7;
    font-weight: 300; margin-bottom: 24px;
    max-width: 560px;
  }

  .project-card {
    background: var(--dark-3);
    border: 1px solid rgba(255,255,255,0.06);
    border-radius: 20px; padding: 32px 28px;
    transition: all 0.4s;
    position: relative; overflow: hidden;
    display: flex; flex-direction: column;
  }

  .project-card::before {
    content: '';
    position: absolute; top: 0; left: 0; right: 0; height: 3px;
    background: var(--card-color, var(--turquesa));
    transform: scaleX(0);
    transition: transform 0.4s;
    transform-origin: left;
  }

  .project-card:hover {
    transform: translateY(-7px);
    border-color: rgba(0,180,216,0.18);
    box-shadow: 0 28px 56px rgba(0,0,0,0.3);
  }

  .project-card:hover::before { transform: scaleX(1); }

  .project-emoji { font-size: 2.2rem; margin-bottom: 18px; display: block; }

  .project-name {
    font-family: 'Syne', sans-serif;
    font-size: 1.2rem; font-weight: 700;
    margin-bottom: 10px;
  }

  .project-desc {
    color: var(--gray-light);
    font-size: 0.9rem; line-height: 1.7;
    font-weight: 300; flex: 1;
    margin-bottom: 24px;
  }

  .project-btn {
    display: block; text-align: center;
    text-decoration: none;
    padding: 12px 20px; border-radius: 10px;
    font-weight: 700; font-size: 0.82rem;
    font-family: 'Syne', sans-serif;
    letter-spacing: 0.5px;
    transition: all 0.28s;
    margin-top: auto;
  }

  .project-btn-teal {
    background: var(--turquesa); color: var(--dark);
  }
  .project-btn-teal:hover { background: var(--white); }

  .project-btn-gold {
    background: #D4A017; color: #fff;
  }
  .project-btn-gold:hover { background: #e8b520; transform: translateY(-2px); }

  .project-btn-red {
    background: #eb2700; color: #fff;
  }
  .project-btn-red:hover { background: #ff3a10; transform: translateY(-2px); }

  .project-coming {
    text-align: center; color: var(--gray);
    font-weight: 600; font-size: 0.82rem;
    padding: 12px; border: 1px dashed rgba(74,74,74,0.5);
    border-radius: 10px; margin-top: auto;
    letter-spacing: 1px;
  }

  /* ── SOBRE MÍ ── */
  .about {
    background: var(--dark);
    display: grid; grid-template-columns: 1fr 1.5fr;
    gap: 80px; align-items: center;
    max-width: 1100px; margin: 0 auto;
    padding: 100px 5%;
  }

  .about-image-wrap {
    position: relative; text-align: center;
  }

  .about-image-wrap img {
    width: 100%; max-width: 380px;
    border-radius: 20px;
    border: 2px solid rgba(0,180,216,0.35);
    box-shadow: 0 20px 50px rgba(0,162,199,0.2);
    display: block; margin: 0 auto;
    object-fit: cover;
  }

  .about-image-placeholder {
    width: 100%; max-width: 380px;
    aspect-ratio: 3/4;
    background: var(--dark-3);
    border-radius: 20px;
    border: 2px dashed rgba(0,180,216,0.3);
    display: flex; flex-direction: column;
    align-items: center; justify-content: center;
    margin: 0 auto; gap: 12px;
    color: var(--gray);
  }

  .about-image-placeholder span { font-size: 3rem; }
  .about-image-placeholder p { font-size: 0.85rem; text-align: center; padding: 0 20px; }

  .about-corner {
    position: absolute; top: -12px; right: calc(50% - 202px);
    width: 80px; height: 80px;
    border-top: 3px solid var(--gold);
    border-right: 3px solid var(--gold);
    border-radius: 0 16px 0 0;
  }

  .about-name {
    font-family: 'Syne', sans-serif;
    font-size: 2.4rem; font-weight: 800;
    color: var(--turquesa); margin-bottom: 6px;
  }

  .about-role {
    color: var(--gold); font-size: 1.15rem;
    font-weight: 400; margin-bottom: 28px;
  }

  .about-text {
    color: var(--gray-light);
    font-size: 1.05rem; line-height: 1.8;
    font-weight: 300; margin-bottom: 18px;
  }

  /* ── AI SECTION ── */
  .ai-wrapper {
    background: var(--dark-2);
    padding: 100px 5%;
  }

  .ai-section {
    display: grid; grid-template-columns: 1fr 1fr;
    gap: 80px; align-items: center;
    max-width: 1100px; margin: 0 auto;
  }

  .ai-features {
    display: flex; flex-direction: column;
    gap: 26px; margin-top: 36px;
  }

  .ai-feature {
    display: flex; gap: 18px; align-items: flex-start;
  }

  .ai-feature-icon {
    width: 46px; height: 46px;
    background: rgba(0,180,216,0.08);
    border: 1px solid rgba(0,180,216,0.2);
    border-radius: 12px;
    display: flex; align-items: center; justify-content: center;
    font-size: 1.25rem; flex-shrink: 0;
  }

  .ai-feature-text h4 {
    font-family: 'Syne', sans-serif;
    font-weight: 700; margin-bottom: 5px; font-size: 0.98rem;
  }

  .ai-feature-text p {
    color: var(--gray-light);
    font-size: 0.87rem; line-height: 1.65; font-weight: 300;
  }

  .ai-card {
    background: var(--dark-3);
    border: 1px solid rgba(0,180,216,0.2);
    border-radius: 22px; padding: 36px;
    position: relative; overflow: hidden;
  }

  .ai-card::before {
    content: '';
    position: absolute; top: -60%; left: -60%;
    width: 220%; height: 220%;
    background: radial-gradient(circle at center, rgba(0,180,216,0.05), transparent 55%);
  }

  .chat-label {
    font-size: 0.72rem; color: var(--gray);
    text-transform: uppercase; letter-spacing: 1px;
    margin-bottom: 18px; font-weight: 600;
  }

  .chat-bubble {
    background: rgba(0,180,216,0.08);
    border: 1px solid rgba(0,180,216,0.18);
    border-radius: 16px 16px 16px 4px;
    padding: 14px 18px; font-size: 0.88rem;
    color: var(--white); margin-bottom: 12px;
    position: relative; z-index: 1;
    max-width: 86%; line-height: 1.55;
  }

  .chat-bubble.user {
    background: var(--turquesa); color: var(--dark);
    border-radius: 16px 16px 4px 16px;
    margin-left: auto; font-weight: 600;
  }

  .chat-typing {
    display: flex; gap: 5px; padding: 14px 18px;
    background: rgba(0,180,216,0.06);
    border: 1px solid rgba(0,180,216,0.12);
    border-radius: 16px 16px 16px 4px;
    width: fit-content; position: relative; z-index: 1;
  }

  .chat-typing span {
    width: 6px; height: 6px; background: var(--turquesa);
    border-radius: 50%; animation: typing 1.2s infinite;
  }
  .chat-typing span:nth-child(2) { animation-delay: 0.2s; }
  .chat-typing span:nth-child(3) { animation-delay: 0.4s; }

  @keyframes typing {
    0%, 60%, 100% { opacity: 0.3; transform: translateY(0); }
    30% { opacity: 1; transform: translateY(-4px); }
  }

  /* ── PLANS ── */
  .plans { background: var(--dark); }

  .plans-header { text-align: center; margin-bottom: 56px; }
  .plans-header .section-sub { margin: 0 auto; }

  .plans-grid {
    display: grid; grid-template-columns: repeat(3, 1fr);
    gap: 22px; max-width: 980px; margin: 0 auto;
    align-items: center;
  }

  .plan-card {
    background: var(--dark-3);
    border: 1px solid rgba(255,255,255,0.07);
    border-radius: 20px; padding: 36px 28px;
    transition: all 0.4s; position: relative;
    display: flex; flex-direction: column;
  }

  .plan-card:hover {
    transform: translateY(-6px);
    box-shadow: 0 28px 56px rgba(0,0,0,0.3);
  }

  .plan-card.featured {
    border-color: var(--turquesa);
    background: linear-gradient(160deg, var(--dark-4), rgba(0,180,216,0.07));
    transform: scale(1.04);
    box-shadow: 0 0 0 1px rgba(0,180,216,0.3), 0 24px 48px rgba(0,180,216,0.12);
  }

  .plan-card.featured:hover { transform: scale(1.04) translateY(-6px); }

  .plan-badge {
    position: absolute; top: -13px; left: 50%;
    transform: translateX(-50%);
    background: var(--turquesa); color: var(--dark);
    padding: 3px 16px; border-radius: 100px;
    font-size: 0.68rem; font-weight: 800;
    letter-spacing: 1.2px; text-transform: uppercase;
    white-space: nowrap;
  }

  .plan-name {
    font-family: 'Syne', sans-serif; font-size: 0.78rem;
    font-weight: 700; color: var(--gray);
    text-transform: uppercase; letter-spacing: 2px;
    margin-bottom: 12px;
  }

  .plan-price {
    font-family: 'Syne', sans-serif;
    font-size: 3rem; font-weight: 800;
    color: var(--white); line-height: 1; margin-bottom: 6px;
  }
  .plan-price span { font-size: 1rem; color: var(--gray); font-weight: 400; }

  .plan-desc {
    color: var(--gray); font-size: 0.84rem;
    margin-bottom: 24px; padding-bottom: 24px;
    border-bottom: 1px solid rgba(255,255,255,0.06);
    font-weight: 300; line-height: 1.6;
  }

  .plan-features {
    list-style: none; display: flex;
    flex-direction: column; gap: 10px;
    margin-bottom: 28px; flex: 1;
  }

  .plan-features li {
    font-size: 0.87rem; color: var(--white);
    display: flex; gap: 10px; align-items: flex-start;
    line-height: 1.5;
  }

  .plan-features li::before {
    content: '✓'; color: var(--turquesa);
    font-weight: 700; flex-shrink: 0; margin-top: 1px;
  }

  .plan-btn {
    display: block; text-align: center;
    padding: 13px; border-radius: 12px;
    font-weight: 700; font-size: 0.88rem;
    text-decoration: none; transition: all 0.28s;
    font-family: 'Syne', sans-serif;
  }

  .plan-btn-outline {
    border: 1.5px solid rgba(255,255,255,0.14);
    color: var(--white);
  }
  .plan-btn-outline:hover { border-color: var(--turquesa); color: var(--turquesa); }

  .plan-btn-filled {
    background: var(--turquesa); color: var(--dark);
  }
  .plan-btn-filled:hover { background: var(--white); transform: translateY(-2px); }

  /* ── MARIA ── */
  .maria-section {
    background: linear-gradient(160deg, var(--dark-3), var(--dark-2));
    text-align: center; position: relative; overflow: hidden;
    padding: 100px 5%;
  }

  .maria-section::before {
    content: '';
    position: absolute; inset: 0;
    background: radial-gradient(ellipse 80% 70% at 50% 50%, rgba(0,180,216,0.07), transparent 70%);
  }

  .maria-content { position: relative; z-index: 1; max-width: 520px; margin: 0 auto; }

  .maria-avatar {
    width: 88px; height: 88px;
    background: linear-gradient(135deg, var(--turquesa), var(--turquesa-deep));
    border-radius: 50%;
    display: flex; align-items: center; justify-content: center;
    font-size: 2.3rem; margin: 0 auto 22px;
    border: 3px solid rgba(0,180,216,0.3);
    box-shadow: 0 0 50px rgba(0,180,216,0.2);
  }

  .maria-section h2 {
    font-family: 'Syne', sans-serif;
    font-size: clamp(2rem, 4vw, 3.2rem);
    font-weight: 800; margin-bottom: 16px;
  }

  .maria-section p {
    color: var(--gray-light); font-size: 1.05rem;
    margin-bottom: 36px; line-height: 1.75; font-weight: 300;
  }

  /* ── CONTACT ── */
  .contact { background: var(--dark-2); }

  .contact-inner {
    max-width: 1100px; margin: 0 auto;
    display: grid; grid-template-columns: 1fr 1fr;
    gap: 80px; align-items: start;
  }

  .contact-info-items {
    display: flex; flex-direction: column; gap: 24px; margin-top: 36px;
  }

  .contact-form {
    background: var(--dark-3);
    border: 1px solid rgba(255,255,255,0.06);
    border-radius: 20px; padding: 36px;
    display: flex; flex-direction: column; gap: 18px;
  }

  .form-group {
    display: flex; flex-direction: column; gap: 7px;
  }

  .form-group label {
    font-size: 0.75rem; text-transform: uppercase;
    letter-spacing: 1.2px; color: var(--gray);
    font-weight: 600;
  }

  .form-group input,
  .form-group select,
  .form-group textarea {
    background: var(--dark-4);
    border: 1px solid rgba(255,255,255,0.08);
    border-radius: 10px; padding: 13px 16px;
    color: var(--white);
    font-family: 'DM Sans', sans-serif;
    font-size: 0.93rem;
    transition: border-color 0.25s, box-shadow 0.25s;
    outline: none;
    width: 100%;
    -webkit-appearance: none;
  }

  .form-group input:focus,
  .form-group select:focus,
  .form-group textarea:focus {
    border-color: var(--turquesa);
    box-shadow: 0 0 0 3px rgba(0,180,216,0.08);
  }

  .form-group input::placeholder,
  .form-group textarea::placeholder { color: rgba(122,143,173,0.5); }

  .form-group select option { background: var(--dark-3); color: var(--white); }

  .form-group textarea { resize: vertical; min-height: 110px; }

  .form-submit {
    background: var(--turquesa); color: var(--dark);
    border: none; padding: 15px 36px;
    border-radius: 100px; font-weight: 700;
    font-size: 0.95rem; cursor: pointer;
    font-family: 'Syne', sans-serif;
    transition: all 0.28s; align-self: flex-start;
    letter-spacing: 0.3px;
  }

  .form-submit:hover {
    background: var(--white);
    transform: translateY(-3px);
    box-shadow: 0 15px 30px rgba(0,180,216,0.28);
  }

  .form-submit.success {
    background: #2DC653; color: #fff; transform: none;
  }

  /* ── FOOTER ── */
  footer {
    background: var(--dark);
    padding: 52px 5%;
    border-top: 1px solid rgba(255,255,255,0.05);
  }

  .footer-inner {
    max-width: 1100px; margin: 0 auto;
    display: flex; justify-content: space-between;
    align-items: center; flex-wrap: wrap; gap: 24px;
  }

  .footer-logo {
    font-family: 'Syne', sans-serif; font-size: 1.25rem;
    font-weight: 800; color: var(--turquesa);
    text-decoration: none;
  }

  .footer-links {
    display: flex; gap: 28px; list-style: none; flex-wrap: wrap;
  }

  .footer-links a {
    color: var(--gray); text-decoration: none;
    font-size: 0.84rem; transition: color 0.25s;
  }
  .footer-links a:hover { color: var(--turquesa); }

  .footer-copy { color: var(--gray); font-size: 0.78rem; letter-spacing: 0.5px; }

  /* ── ANIMATIONS ── */
  @keyframes fadeInDown {
    from { opacity: 0; transform: translateY(-18px); }
    to { opacity: 1; transform: translateY(0); }
  }
  @keyframes fadeInUp {
    from { opacity: 0; transform: translateY(26px); }
    to { opacity: 1; transform: translateY(0); }
  }

  .reveal {
    opacity: 0; transform: translateY(28px);
    transition: opacity 0.6s ease, transform 0.6s ease;
  }
  .reveal.visible { opacity: 1; transform: translateY(0); }

  /* ── RESPONSIVE ── */
  @media (max-width: 900px) {
    .about { grid-template-columns: 1fr; gap: 48px; padding: 80px 5%; }
    .about-image-wrap { max-width: 320px; margin: 0 auto; }
    .ai-section { grid-template-columns: 1fr; gap: 48px; }
    .plans-grid { grid-template-columns: 1fr; max-width: 420px; }
    .plan-card.featured { transform: scale(1); }
    .plan-card.featured:hover { transform: translateY(-6px); }
    .contact-inner { grid-template-columns: 1fr; gap: 48px; }
    .projects-grid { grid-template-columns: 1fr; }
    .project-card-featured { flex-direction: column; gap: 20px; padding: 28px; }
  }

  @media (max-width: 768px) {
    nav { padding: 14px 5%; }
    .nav-links { display: none; }
    .nav-toggle { display: flex; }
    section { padding: 72px 5%; }
    .hero { padding: 100px 5% 70px; }
    .stats { padding: 36px 5%; }
    .stat { padding: 14px 24px; }
    .stat:not(:last-child)::after { display: none; }
    .footer-inner { flex-direction: column; text-align: center; }
    .footer-links { justify-content: center; }
  }

  /* ── MOBILE MENU ── */
  .mobile-menu {
    display: none; position: fixed;
    top: 72px; left: 0; right: 0;
    background: rgba(3,7,30,0.97);
    backdrop-filter: blur(24px);
    border-bottom: 1px solid rgba(0,180,216,0.1);
    padding: 24px 5%; z-index: 190;
    flex-direction: column; gap: 20px;
  }

  .mobile-menu.open { display: flex; }

  .mobile-menu a {
    color: var(--white); text-decoration: none;
    font-size: 1rem; font-weight: 500;
    padding: 8px 0;
    border-bottom: 1px solid rgba(255,255,255,0.06);
    transition: color 0.25s;
  }
  .mobile-menu a:hover { color: var(--turquesa); }
  .mobile-menu a:last-child { border: none; }

  .mobile-cta {
    background: var(--turquesa) !important;
    color: var(--dark) !important;
    padding: 13px 20px !important;
    border-radius: 10px; text-align: center;
    font-weight: 700 !important; border: none !important;
  }
</style>
</head>
<body>

<div class="cursor" id="cursor"></div>
<div class="cursor-ring" id="cursorRing"></div>

<!-- ── NAV ── -->
<nav>
  <a href="#" class="nav-logo">Maru<span>ProyectAI</span></a>
  <ul class="nav-links">
    <li><a href="#proyectos">Proyectos</a></li>
    <li><a href="#sobre-mi">Sobre Mí</a></li>
    <li><a href="#planes">Planes</a></li>
    <li><a href="#contacto">Contacto</a></li>
    <li><a href="https://bit.ly/MaruProyectAI" target="_blank" class="nav-cta">Hablar con MarIA</a></li>
  </ul>
  <div class="nav-toggle" id="navToggle" aria-label="Menú">
    <span></span><span></span><span></span>
  </div>
</nav>

<div class="mobile-menu" id="mobileMenu">
  <a href="#proyectos">Proyectos</a>
  <a href="#sobre-mi">Sobre Mí</a>
  <a href="#planes">Planes</a>
  <a href="#contacto">Contacto</a>
  <a href="https://bit.ly/MaruProyectAI" target="_blank" class="mobile-cta">💬 Hablar con MarIA</a>
</div>

<!-- ── HERO ── -->
<section class="hero">
  <div class="hero-grid"></div>
  <div class="hero-orb-1"></div>
  <div class="hero-orb-2"></div>
  <div class="hero-content">
    <div class="hero-badge">
      <div class="badge-dot"></div>
      Ecosistema Digital LATAM — Activo 24/7
    </div>
    <h1>
      Transformo negocios con<br>
      <span class="highlight">Inteligencia Artificial</span><br>
      desde LATAM para el mundo
    </h1>
    <p class="hero-sub">
      Soy Maru Rodriguez — Founder, Prompt Engineer y Local Guide Nivel 8.
      Creo ecosistemas digitales que trabajan por ti mientras duermes.
    </p>
    <div class="hero-ctas">
      <a href="https://bit.ly/MaruProyectAI" target="_blank" class="btn-primary">
        💬 Hablar con MarIA
      </a>
      <a href="#proyectos" class="btn-secondary">Ver mis proyectos →</a>
    </div>
  </div>
</section>

<!-- ── STATS ── -->
<div class="stats">
  <div class="stat">
    <span class="stat-number">+5.7M</span>
    <span class="stat-label">Vistas en Google Maps</span>
  </div>
  <div class="stat">
    <span class="stat-number">+900</span>
    <span class="stat-label">Lugares documentados</span>
  </div>
  <div class="stat">
    <span class="stat-number">12 años</span>
    <span class="stat-label">De trayectoria digital</span>
  </div>
  <div class="stat">
    <span class="stat-number">4</span>
    <span class="stat-label">Proyectos activos</span>
  </div>
  <div class="stat">
    <span class="stat-number">24/7</span>
    <span class="stat-label">Bots operativos</span>
  </div>
</div>

<!-- ── PROJECTS ── -->
<section class="projects" id="proyectos">
  <div class="projects-header">
    <div class="section-badge">Mis proyectos</div>
    <h2 class="section-title">Un ecosistema,<br>cuatro fortalezas</h2>
    <p class="section-sub">Cada proyecto tiene su propio agente de IA especializado, disponible para ayudarte en cualquier momento.</p>
  </div>

  <div class="projects-grid">

    <!-- Featured -->
    <div class="project-card-featured reveal">
      <div class="featured-emoji">✨</div>
      <div class="featured-body">
        <div class="project-name">MaruProyectAI</div>
        <p class="project-desc">Ecosistema de soluciones inteligentes. Transformamos tu visión empresarial con agentes de IA personalizados y automatización de vanguardia. El nodo central que conecta todo.</p>
        <a href="https://bit.ly/MaruProyectAI" target="_blank" class="project-btn project-btn-teal" style="display:inline-flex; align-items:center; gap:8px; width:auto;">
          💬 Hablar con MarIA
        </a>
      </div>
    </div>

    <!-- VeneHome -->
    <div class="project-card reveal" style="--card-color: #D4A017;">
      <span class="project-emoji">🏗️</span>
      <div class="project-name">VeneHome & Renórica</div>
      <p class="project-desc">Asesoría experta en construcción, remodelación y energía solar, potenciada por IA. Desde el diseño hasta la instalación.</p>
      <a href="https://bit.ly/BobIA" target="_blank" class="project-btn project-btn-gold">Bot BobIA →</a>
    </div>

    <!-- Raqueta Viajera -->
    <div class="project-card reveal" style="--card-color: #eb2700;">
      <span class="project-emoji">🎾</span>
      <div class="project-name">Raqueta Viajera</div>
      <p class="project-desc">Tu conserje inteligente para turismo deportivo y torneos de tenis, pádel y pickleball alrededor del mundo.</p>
      <a href="https://bit.ly/RacketAI" target="_blank" class="project-btn project-btn-red">Bot RacketAI →</a>
    </div>

    <!-- LocalAgent -->
    <div class="project-card reveal" style="--card-color: #4A4A4A; opacity: 0.85;">
      <span class="project-emoji">🤖</span>
      <div class="project-name">LocalAgent AI</div>
      <p class="project-desc">Gestión inteligente de reseñas y mensajes para negocios locales. Respuesta automática 24/7 en Google Maps e Instagram.</p>
      <div class="project-coming">https://bit.ly/LocalMapAI</div>
    </div>

  </div>
</section>

<!-- ── SOBRE MÍ ── -->
<section id="sobre-mi" style="background: var(--dark); padding: 0;">
  <div class="about">
    <div class="about-image-wrap reveal">
      <div class="about-corner"></div>
      <!-- Reemplaza este bloque con <img src="Maru-Pensativa.jpg" alt="Maru Rodriguez"> cuando tengas la foto -->
      <div class="about-image-placeholder">
        <span>📸</span>
        <p>Tu foto aquí — reemplaza este bloque por un &lt;img&gt;</p>
      </div>
    </div>
    <div class="reveal">
      <div class="section-badge">Sobre mí</div>
      <h2 class="about-name">Maru Rodriguez</h2>
      <p class="about-role">Fundadora de MaruProyectAI</p>
      <p class="about-text">
        Soy una apasionada de la innovación y la transformación digital. Mi objetivo con <strong>MaruProyectAI</strong> es crear un puente entre la tecnología de vanguardia y las necesidades reales de las empresas.
      </p>
      <p class="about-text">
        A través de proyectos como <strong>VeneHome Group</strong> y <strong>Raqueta Viajera</strong>, aprendí que la clave del éxito no está solo en la Inteligencia Artificial, sino en cómo esta se adapta para servir a las personas. Aquí encontrarás soluciones diseñadas con visión, propósito y un toque humano.
      </p>
    </div>
  </div>
</section>

<!-- ── IA SECTION ── -->
<div class="ai-wrapper">
  <div class="ai-section" id="ia">
    <div class="reveal">
      <div class="section-badge">Inteligencia Artificial</div>
      <h2 class="section-title">¿Por qué tu negocio<br>necesita IA ahora?</h2>
      <p class="section-sub">El 78% de los clientes espera respuesta inmediata. Cada minuto sin responder es un cliente que se va a la competencia.</p>
      <div class="ai-features">
        <div class="ai-feature">
          <div class="ai-feature-icon">⚡</div>
          <div class="ai-feature-text">
            <h4>Respuesta en segundos, no en horas</h4>
            <p>Tu agente responde reseñas de Google Maps y DMs de Instagram al instante, los 365 días del año.</p>
          </div>
        </div>
        <div class="ai-feature">
          <div class="ai-feature-icon">🎯</div>
          <div class="ai-feature-text">
            <h4>Captura leads automáticamente</h4>
            <p>Cada conversación termina con el nombre y WhatsApp del cliente potencial. Nunca más pierdas una oportunidad.</p>
          </div>
        </div>
        <div class="ai-feature">
          <div class="ai-feature-icon">📈</div>
          <div class="ai-feature-text">
            <h4>Métricas en tiempo real</h4>
            <p>Dashboard con usuarios, conversaciones y rendimiento. Toma decisiones basadas en datos reales.</p>
          </div>
        </div>
        <div class="ai-feature">
          <div class="ai-feature-icon">🌎</div>
          <div class="ai-feature-text">
            <h4>Personalidad de marca única</h4>
            <p>Tu agente habla como tu marca, conoce tus productos y escala a ti solo cuando es necesario.</p>
          </div>
        </div>
      </div>
    </div>
    <div class="ai-visual reveal">
      <div class="ai-card">
        <div class="chat-label">LexIA en acción — demo en vivo</div>
        <div class="chat-bubble user">Hola, ¿tienen servicio en Bogotá?</div>
        <div class="chat-bubble">¡Hola! Soy LexIA 🤖 Sí, operamos en toda Colombia y LATAM. ¿Qué tipo de negocio tienes? Te cuento cómo podemos ayudarte.</div>
        <div class="chat-bubble user">Tengo una clínica dental</div>
        <div class="chat-bubble">¡Perfecto! Para clínicas dentales tenemos el plan Growth a $79/mes — respuesta automática en Google Maps + DMs de Instagram. ¿Quieres una demo gratuita? Dame tu WhatsApp 📱</div>
        <div class="chat-typing">
          <span></span><span></span><span></span>
        </div>
      </div>
    </div>
  </div>
</div>

<!-- ── PLANS ── -->
<section class="plans" id="planes">
  <div class="plans-header">
    <div class="section-badge">LocalAgent AI</div>
    <h2 class="section-title">Planes para cada negocio</h2>
    <p class="section-sub">Empieza hoy. Cancela cuando quieras. Tu primer mes es tu prueba.</p>
  </div>
  <div class="plans-grid">

    <div class="plan-card reveal">
      <div class="plan-name">Starter</div>
      <div class="plan-price">$29<span>/mes</span></div>
      <p class="plan-desc">Para negocios que están comenzando con IA</p>
      <ul class="plan-features">
        <li>1 agente de IA personalizado</li>
        <li>Google Maps o Instagram</li>
        <li>500 respuestas/mes</li>
        <li>Captura de leads básica</li>
        <li>Soporte por email</li>
      </ul>
      <a href="https://bit.ly/MaruProyectAI" target="_blank" class="plan-btn plan-btn-outline">Comenzar ahora</a>
    </div>

    <div class="plan-card featured reveal">
      <div class="plan-badge">Más Popular</div>
      <div class="plan-name">Growth</div>
      <div class="plan-price">$79<span>/mes</span></div>
      <p class="plan-desc">Para negocios que quieren crecer con IA avanzada</p>
      <ul class="plan-features">
        <li>2 agentes de IA personalizados</li>
        <li>Google Maps + Instagram</li>
        <li>2,000 respuestas/mes</li>
        <li>CRM de leads integrado</li>
        <li>Dashboard de métricas</li>
        <li>Soporte prioritario</li>
      </ul>
      <a href="https://bit.ly/MaruProyectAI" target="_blank" class="plan-btn plan-btn-filled">Comenzar ahora</a>
    </div>

    <div class="plan-card reveal">
      <div class="plan-name">Agency</div>
      <div class="plan-price">$199<span>/mes</span></div>
      <p class="plan-desc">Para agencias y negocios con múltiples sucursales</p>
      <ul class="plan-features">
        <li>Agentes ilimitados</li>
        <li>Todos los canales</li>
        <li>Respuestas ilimitadas</li>
        <li>White label disponible</li>
        <li>API access</li>
        <li>Soporte dedicado 24/7</li>
      </ul>
      <a href="https://bit.ly/MaruProyectAI" target="_blank" class="plan-btn plan-btn-outline">Hablar con ventas</a>
    </div>

  </div>
</section>

<!-- ── MARIA ── -->
<section class="maria-section">
  <div class="maria-content reveal">
    <div class="maria-avatar">🤖</div>
    <div class="section-badge">Tu asistente virtual</div>
    <h2>Hola, soy MarIA</h2>
    <p>Tu asistente virtual disponible 24/7 para orientarte sobre todos los proyectos de Maru Rodriguez. Cuéntame qué necesitas.</p>
    <a href="https://bit.ly/MaruProyectAI" target="_blank" class="btn-primary">
      💬 Iniciar conversación con MarIA
    </a>
  </div>
</section>

<!-- ── CONTACT ── -->
<section class="contact" id="contacto">
  <div class="contact-inner">
    <div class="reveal">
      <div class="section-badge">Contacto</div>
      <h2 class="section-title">¿Listo para<br>transformar tu negocio?</h2>
      <p class="section-sub">Cuéntanos sobre tu proyecto y te contactamos en menos de 24 horas con una propuesta personalizada.</p>
      <div class="contact-info-items">
        <div class="ai-feature">
          <div class="ai-feature-icon">📧</div>
          <div class="ai-feature-text">
            <h4>Email</h4>
            <p>virtuallatinoamericana@gmail.com</p>
          </div>
        </div>
        <div class="ai-feature">
          <div class="ai-feature-icon">📱</div>
          <div class="ai-feature-text">
            <h4>WhatsApp</h4>
            <p>+57 301 508 0335</p>
          </div>
        </div>
        <div class="ai-feature">
          <div class="ai-feature-icon">📍</div>
          <div class="ai-feature-text">
            <h4>Ubicación</h4>
            <p>Bogotá, Colombia — Operando en LATAM</p>
          </div>
        </div>
      </div>
    </div>

    <form class="contact-form reveal" id="contactForm" name="contacto" method="POST" action="/" data-netlify="true">
      <input type="hidden" name="form-name" value="contacto">
      <div class="form-group">
        <label for="nombre">Nombre Completo</label>
        <input type="text" id="nombre" name="nombre" placeholder="María González" required>
      </div>
      <div class="form-group">
        <label for="whatsapp">WhatsApp</label>
        <input type="tel" id="whatsapp" name="whatsapp" placeholder="+57 300 000 0000" required>
      </div>
      <div class="form-group">
        <label for="email">Correo Electrónico</label>
        <input type="email" id="email" name="email" placeholder="tu@email.com" required>
      </div>
      <div class="form-group">
        <label for="proyecto">¿Qué proyecto te interesa?</label>
        <select id="proyecto" name="proyecto" required>
          <option value="">Selecciona una opción</option>
          <option value="LocalAgent AI">LocalAgent AI — Agentes de IA para mi negocio</option>
          <option value="VeneHome Group">VeneHome Group — Construcción o remodelación</option>
          <option value="Renorica">Renórica — Energía solar</option>
          <option value="Raqueta Viajera">Raqueta Viajera — Turismo deportivo</option>
        </select>
      </div>
      <div class="form-group">
        <label for="mensaje">Cuéntanos más</label>
        <textarea id="mensaje" name="mensaje" placeholder="Describe brevemente tu proyecto o necesidad..."></textarea>
      </div>
      <button type="submit" class="form-submit" id="submitBtn">Quiero empezar ya →</button>
    </form>
  </div>
</section>

<!-- ── FOOTER ── -->
<footer>
  <div class="footer-inner">
    <a href="#" class="footer-logo">MaruProyectAI</a>
    <ul class="footer-links">
      <li><a href="#proyectos">Proyectos</a></li>
      <li><a href="#planes">Planes</a></li>
      <li><a href="#contacto">Contacto</a></li>
      <li><a href="https://bit.ly/MaruProyectAI" target="_blank" style="color: var(--turquesa); font-weight: 600;">MarIA</a></li>
    </ul>
    <p class="footer-copy">© 2026 MaruProyectAI — Maru Rodriguez | Bogotá, Colombia</p>
  </div>
</footer>

<script>
  // ── Custom cursor
  const cursor = document.getElementById('cursor');
  const ring = document.getElementById('cursorRing');

  document.addEventListener('mousemove', (e) => {
    cursor.style.left = (e.clientX - 5) + 'px';
    cursor.style.top  = (e.clientY - 5) + 'px';
    setTimeout(() => {
      ring.style.left = (e.clientX - 17) + 'px';
      ring.style.top  = (e.clientY - 17) + 'px';
    }, 60);
  });

  document.querySelectorAll('a, button').forEach(el => {
    el.addEventListener('mouseenter', () => {
      cursor.style.transform = 'scale(2.2)';
      ring.style.transform = 'scale(1.4)';
      ring.style.borderColor = 'var(--turquesa)';
    });
    el.addEventListener('mouseleave', () => {
      cursor.style.transform = 'scale(1)';
      ring.style.transform = 'scale(1)';
      ring.style.borderColor = 'rgba(0,180,216,0.6)';
    });
  });

  // ── Mobile menu
  const toggle = document.getElementById('navToggle');
  const menu   = document.getElementById('mobileMenu');

  toggle.addEventListener('click', () => {
    menu.classList.toggle('open');
  });

  menu.querySelectorAll('a').forEach(link => {
    link.addEventListener('click', () => menu.classList.remove('open'));
  });

  // ── Scroll reveal
  const observer = new IntersectionObserver((entries) => {
    entries.forEach(entry => {
      if (entry.isIntersecting) {
        entry.target.classList.add('visible');
        observer.unobserve(entry.target);
      }
    });
  }, { threshold: 0.08 });

  document.querySelectorAll('.reveal').forEach(el => observer.observe(el));

  // ── Contact form (Netlify-compatible)
  const form = document.getElementById('contactForm');
  const btn  = document.getElementById('submitBtn');

  form.addEventListener('submit', async (e) => {
    e.preventDefault();
    btn.textContent = 'Enviando...';
    btn.disabled = true;

    try {
      const data = new FormData(form);
      await fetch('/', {
        method: 'POST',
        headers: { 'Content-Type': 'application/x-www-form-urlencoded' },
        body: new URLSearchParams(data).toString()
      });
      btn.textContent = '✓ Mensaje enviado';
      btn.classList.add('success');
      form.reset();
      setTimeout(() => {
        btn.textContent = 'Quiero empezar ya →';
        btn.classList.remove('success');
        btn.disabled = false;
      }, 4000);
    } catch {
      btn.textContent = 'Reintentar →';
      btn.disabled = false;
    }
  });
</script>
</body>
</html>